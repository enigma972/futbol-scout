{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-1">Enregister</button>
    <button type="button" class="btn btn-secondary mt-1" data-dismiss="modal">Annuler</button>
{{ form_end(form) }}
