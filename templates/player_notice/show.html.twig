{% extends 'base.html.twig' %}

{% block title %}PlayerNotice{% endblock %}

{% block body %}
    <h1>PlayerNotice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Time</th>
                <td>{{ player_notice.time ? player_notice.time|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ player_notice.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('player_notice_index') }}">back to list</a>

    <a href="{{ path('player_notice_edit', {'author': player_notice.author}) }}">edit</a>

    {{ include('player_notice/_delete_form.html.twig') }}
{% endblock %}
